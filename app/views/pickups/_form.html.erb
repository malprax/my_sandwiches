<%= simple_form_for(@pickup) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :times %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
