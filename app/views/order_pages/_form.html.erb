<%= form_for(@order_page, :remote => true)  do |f| %>
  <% if @order_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order_page.errors.count, "error") %> prohibited this order_page from being saved:</h2>

      <ul>
      <% @order_page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  
  <div class="date">
     Date <%=  Time.now.strftime('%d %b, %Y')%>
  </div>
  
  <div class="pickuptime">   
    <%= f.label :pickuptime, id: "pick" %>
    <%= f.collection_select(:pickuptime_id, Pickuptime.order("pickuptime"), :id, :pickuptime ) %>
     
  </div>
  
  <div class="quantity">
    <%= f.label :order_quantity %>
     <%= f.range_field :order_quantity, :in => 1..10, :value => 1%>      
      
     <span class="badge badge-warning"><text id="order_quantity">1</text></span>
  </div>   
  
  <div class="breadtype">   
    <%= f.label :bread_type_id, :id => "bread" %><br />
    <%= f.collection_radio_buttons(:bread_type_id, BreadType.order("name"), :id, :name)%>    
  </div>
  
  <!-- <div class="sandwichtype">   
    <%= f.label :sandwich_type %><br />
    <%= f.radio_button :sandwich_type, false, name: "ordersandwich"%>Standard   
    <%= f.radio_button :sandwich_type,  true, name: "ordersandwich", prompt: "You Can Choose Your Vegetable And Sauce" %>Own
     
  </div> -->
  
  <!-- <div class="spread">   
    <%= f.label :spread_id %><br />
    <%= f.collection_select(:spread_id, Spread.order("name"), :id, :name) %>    
  </div> -->
  
  <div class="spread">
    <%= f.label :spread_id, :id => "labelspread" %><br /> 
    <% Spread.all.each do |spread| %> 
        <%= check_box_tag "spread", spread.id%>
        <%= spread.name %>
    <% end %>      
  </div>
  
  <div class="vegetable">
    <%= f.label :vegetable_id %><br /> 
    <% Vegetable.all.each do |vegetable| %> 
        <%= check_box_tag "vegetable", vegetable.id%>
        <%= vegetable.name %>
    <% end %>      
  </div>
  
  <div class="sauce">
    <%= f.label :sauce_id, :id => "labelsauce" %><br /> 
    <% Sauce.all.each do |sauce| %> 
        <%= check_box_tag "sauce", sauce.id%>
        <%= sauce.name %>
    <% end %>      
  </div>
    
  <!-- <div class="vegetable">   
    <%= f.label :vegetable_id, "Vegetable 1" %><br />
    <%= f.collection_select(:vegetable_id, Vegetable.order("name"), :id, :name) %>  
  </div>
  
  <div class="vegetable">   
    <%= f.label :vegetable_id, "Vegetable 2" %><br />
    <%= f.collection_select(:vegetable_id, Vegetable.order("name"), :id, :name) %>  
  </div>
  
  <div class="vegetable">   
    <%= f.label :vegetable_id, "Vegetable 3" %><br />
    <%= f.collection_select(:vegetable_id, Vegetable.order("name"), :id, :name) %>  
  </div>
  
  <div class="sauce">   
    <%= f.label :sauce_id %><br />
    <%= f.collection_select(:sauce_id, Sauce.order("name"), :id, :name) %>    
  </div>   -->
  
  <div class="comment">
    <%= f.label :comment_id, :id => "comment" %><br />
    <%= f.text_area :comment_id, class:'wysihtml5', id:'some-textarea', hint:"Write Your Comment Here" %>
  </div> 
   
    <div class="actions">
      <%= f.submit 'Next', :class => "btnform"%>
    </div> 
     
<% end %>
