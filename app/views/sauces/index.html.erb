<h1>Listing sauces</h1>

 <% @sauces.each do |sauce| %>
   <%= simple_form for sauce do |f| %>
   <%end %>
 
 <% end %>
<table class="table">
  <thead>
    <tr>
      <th>Item</th>
      <th>Price</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @sauces.each do |sauce| %>
      <tr>
        <td><%= sauce.item %></td>
        <td><%= sauce.price %></td>
        <td><%= link_to 'Show', sauce %></td>
        <td><%= link_to 'Edit', edit_sauce_path(sauce) %></td>
        <td><%= link_to 'Destroy', sauce, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Sauce', new_sauce_path %>
